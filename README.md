# Kanban Board

A simple and interactive Kanban board application built with React, TypeScript, and Vite.  
Organize your tasks into columns like **To Do**, **In Progress**, and **Done** or even you can create a custom columns with an easy drag-and-drop interface.

---

## 📌 How to Set Up

### 1. Clone the repository
To download the project to your local system:

```bash
git clone <repo-url>
cd Kanban-board
```

### 2. Install node-modules
Don't forget to install the node-modules

```bash
npm install 
```

### 3. How to Run
To run the app:

```bash
npm run dev
```